(section
  (ref "openal_extension_efx")
  (title "OpenAL.Extension.EFX")

  (para "The " (item ada_package "OpenAL.Extension.EFX") " package provides
    the " (link-ext "http://connect.creativelabs.com/developer/Wiki/Introduction%20to%20EFX.aspx"
    "EFX") " effects extension.")

  (para "The extension may not be present on all " (item package "OpenAL")
    " implementations. Subprograms are provided to check for the existence
    of the extension and to safely dynamically load it if it is.")

  (contents)

  (include "a-openal-extension-efx-is_present.ud")
  (include "a-openal-extension-efx-load_extension.ud")

  (include "a-openal-extension-efx-generate_effects.ud")
  (include "a-openal-extension-efx-delete_effects.ud")
  (include "a-openal-extension-efx-effect_is_valid.ud")

  (include "a-openal-extension-efx-generate_filters.ud")
  (include "a-openal-extension-efx-delete_filters.ud")
  (include "a-openal-extension-efx-filter_is_valid.ud")

  (include "a-openal-extension-efx-generate_auxiliary_effect_slots.ud")
  (include "a-openal-extension-efx-delete_auxiliary_effect_slots.ud")
  (include "a-openal-extension-efx-auxiliary_effect_slot_is_valid.ud")
)
