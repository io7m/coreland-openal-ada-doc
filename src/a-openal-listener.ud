(section
  (title "OpenAL.Listener")
  (ref "openal_listener")

  (para "The " (item ada_package "OpenAL.Listener") " package provides
    subprograms for manipulating the simulation listener.")

  (contents)

  (include "a-openal-listener-position.ud")
  (include "a-openal-listener-velocity.ud")
  (include "a-openal-listener-orientation.ud")
  (include "a-openal-listener-gain.ud")
)
