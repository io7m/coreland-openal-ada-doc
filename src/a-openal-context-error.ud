(section
  (title "OpenAL.Context.Error")
  (ref "openal_context_error")

  (para "The " (item ada_package "OpenAL.Context.Error") " package provides
    subprograms for retrieving device and context errors.")

  (contents)

  (include "a-openal-context-error-get_error.ud")
)
